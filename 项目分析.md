# GoFrame Skills 项目深度分析报告

> 仓库:`github.com/gogf/skills`(本机路径 `/mnt/e/project/openProject/gf-skill/skills/`)
> 分析维度:项目定位、SKILL.md 质量、数据来源、AI 可用性、与行业最佳实践的差距

---

## 一、项目定位与概述

### 1.1 这是什么项目

**GoFrame Skills** 是 GoFrame 框架官方维护的 **AI 编码 Skill 资源包**。它把 GoFrame 官方文档、组件规范、实战示例打包成符合 [Agent Skills 规范](https://agentskills.io/specification) 的格式,供 Claude Code、Cursor、Copilot 等 AI 编程助手加载使用。

核心目标:**让 AI 在 GoFrame 项目中生成的代码不再"凭印象猜 API",而是严格按 GoFrame 社区规范产出可直接投产的代码**。

### 1.2 解决的问题

- **AI 训练数据滞后**:大模型对 GoFrame 内部约定的掌握有限,容易写出"能跑但不符合社区规范"的代码
- **约定分散**:GoFrame 强约定(gerror、DO 对象、gf init 脚手架、软删除自动维护等)分布在多份文档里,AI 难以一次性掌握
- **示例断层**:官方文档讲 API,examples 仓库讲实战,AI 不知道何时该读哪一份

### 1.3 使用场景

| 场景 | 触发条件 |
|------|---------|
| 新建 GoFrame HTTP 服务 | 用户说"用 GoFrame 写一个 XXX 服务" |
| 新建 GoFrame gRPC 微服务 | 用户涉及 gRPC、etcd、负载均衡等 |
| 给现有 GoFrame 项目加功能 | go.mod 含 `github.com/gogf/gf/v2` |
| 排查 GoFrame 规范相关问题 | "DO 对象怎么用"、"软删除为什么不生效" |
| 学习 GoFrame 最佳实践 | 用户希望参考 canonical 项目结构 |

### 1.4 安装与触发

```bash
npx skills add github.com/gogf/skills -g    # 全局安装
npx skills check                              # 检查更新
npx skills update                             # 一键更新
```

`SKILL.md` 头部 frontmatter 的 `description` 字段已经写得相当精确,**仅在以下证据出现时触发**:
- `go.mod` 引入 `github.com/gogf/gf/v2`
- 代码 import `gogf/gf/v2/...` 包
- 用户明确要求用 GoFrame 脚手架/迁移

明确**不触发**的场景:纯 Go 库、Gin/Echo 项目、前端、Shell、CI/CD 脚本。

---

## 二、核心架构与目录结构

```
gf-skill/
├── README.md                    # 项目说明(给人类看)
├── CONTRIBUTING.md              # 贡献指南
├── SKILL.tpl.md                 # Skill 模板(136 行)
├── main.go                      # Skill 内容生成入口
├── go.mod / go.sum              # 依赖(github.com/gogf/gf/v2)
├── Makefile                     # 含 `make skills` 与 `make up`
├── references/refences.go       # 参考资料生成器(150 行)
├── examples/examples.go         # 示例项目生成器(160 行)
├── skills/goframe-v2/           # 生成的 Skill 产物
│   ├── SKILL.md                 # = SKILL.tpl.md(字节级一致)
│   ├── references/              # 380+ 份清洗后的 Markdown
│   │   ├── README.MD            # 402 行索引表
│   │   ├── 核心组件/             # ORM、日志、配置、错误处理等
│   │   ├── WEB服务开发/         # HTTP、Session、路由等
│   │   ├── 微服务开发/          # gRPC、注册发现、负载均衡
│   │   ├── 开发工具/             # CLI(init/gen/build/run 等)
│   │   └── ...                  # 共 11 大类
│   └── examples/                # 50+ 个真实 Go 项目
│       ├── README.MD            # 53 行索引表
│       ├── practices/           # user-http-service / user-grpc-service 等
│       ├── httpserver/          # jwt、proxy、sse、限流等
│       ├── grpc/                # basic、balancer、resolver、ctx
│       └── ...
└── .github/                     # CI 与 issue 模板
```

**关键观察**:仓库是 **"生成器 + 产物"** 双层结构。
- `SKILL.tpl.md` + `references.go` + `examples.go` + `main.go` 是 **生成器源码**
- `skills/goframe-v2/` 是 **生成出来的最终 Skill 包**,可被 `npx skills` 安装

---

## 三、SKILL.md 内容质量评估

### 3.1 现有 SKILL.md 结构

`SKILL.tpl.md` 共 **136 行**,由 3 部分组成:

| 区块 | 行数 | 性质 |
|------|----:|------|
| YAML frontmatter | 5 | 元数据(name、description、license) |
| Critical Conventions | ~126 | **核心规范正文** |
| 末尾两个链接 | 5 | 指向 `./references/README.MD` 和 `./examples/README.MD` |

**Critical Conventions** 下分 4 节:
- Project Development Standards(gf init 脚手架、禁止手改 dao/do/entity、不用 logic/、参考 user-http-service/user-grpc-service)
- Component Usage Standards(gerror、DO 对象优先、复用优先)
- Code Style Standards(`var (...)` 块对齐、3+ 变量时使用)
- Soft Delete & Time Maintenance(`created_at`/`updated_at`/`deleted_at` 自动维护的反例/正例)

### 3.2 现有 SKILL.md 的优点

1. **`description` 字段是行业标杆**:明确写了"trigger for..."和"do not trigger for...",具体到包路径级别,比 Anthropic 自家的 `pdf` skill 描述更精确。

2. **WRONG / CORRECT 对照示例扎实**:DO 对象、软删除、时间字段都有清晰的反例/正例对比,**强约束胜过松散说明**。

3. **结构层级清晰**:Project / Component / Code Style / Soft Delete 四节分明,易读。

4. **长度合规**:136 行,远低于 Agent Skills 规范推荐的 500 行上限。

5. **frontmatter 完整**:name、description、license 三字段齐全。

### 3.3 关键缺陷(与行业最佳实践对比)

通过对比 [anthropics/skills](https://github.com/anthropics/skills)、[vercel-labs/agent-skills](https://github.com/vercel-labs/agent-skills) 与 [agentskills.io 规范](https://agentskills.io/specification),发现以下结构性缺失:

| 缺失项 | 影响 | 行业标杆 |
|--------|------|---------|
| **无"When to apply"正文** | 仅 frontmatter 有触发条件,正文未重申 | `react-best-practices`、`doc-coauthoring`、`webapp-testing` 都有 |
| **无 references 路由映射** | AI 拿到 402 行索引表却不知道何时读哪一份 | `internal-comms` 用 32 行做"type → examples/xxx.md"的路由表 |
| **无 examples 路由映射** | 50+ 示例项目无导航,AI 不确定何时参考 user-http-service | 同上 |
| **无决策树/分阶段流程** | gf init → gf gen → 业务实现 这种线性流程未结构化 | `mcp-builder` 有 4 阶段分阶段指引 |
| **无失败兜底话术** | 项目不是 GoFrame 时,AI 没有"应该这样回答用户"的指引 | `vercel-optimize` 有完整的"stop and ask"段落 |
| **无工具使用指引** | 没告诉 AI 应该 grep 什么、读哪个子目录 | `webapp-testing` 明确写"Always run scripts with `--help` first" |
| **无"expected user prompts"示例** | 用户可能说"加 JWT 鉴权"、"加 gRPC 客户端",SKILL.md 没列举典型 prompt | 多个 skills 在 README 或 SKILL 中有 |
| **正文未强调自动时间/软删除外的"goFrame 特异性"** | ORM/CRUD 外的 HTTP、配置、中间件、错误处理约定**完全没有** | — |

**结论**:SKILL.md 在 **"代码规范教学"** 这件事上做得很到位,但在 **"如何让 AI 在 400 份参考文档 + 50 个示例项目中找到正确的入口"** 这件事上**严重缺位**。

---

## 四、references/ 与 examples/ 的数据来源分析

通过阅读 `references/refences.go` 和 `examples/examples.go`,可以清晰还原数据流向。

### 4.1 references/(参考文档)— 有清洗,只针对 .md

`main.go` 中硬编码的扫描源:`/Users/john/Workspace/github/gogf/gf-site/docs/docs/` 与 `/docs/quick/`(对应 [github.com/gogf/gf-site](https://github.com/gogf/gf-site) 仓库)。

清洗流程(完整还原):

| 处理动作 | 实现位置 | 说明 |
|---------|---------|------|
| 递归扫描两层目录 | `docFolders = []string{"docs", "quick"}` | — |
| 只处理 .md 文件 | `gstr.InArray([]string{"md", "MD"}, extName)` | — |
| **解析 YAML front matter**,提取 `description` | `getFrontMatter()` 用 `gyaml.Decode` | description 用于首页索引表 |
| 跳过 `notSkillReference: true` 的文档 | `gconv.Bool(frontMatter[notSkillReference])` | 给上游文档留过滤口 |
| 跳过正文 < 200 字符 | `len(mainContent) < minLengthOfMainContent` | — |
| 跳过正文 ≤ 10 行 | `gstr.Count(mainContent, "\n") <= 10` | — |
| **剥离 front matter 块** | 正则 `^---\n([\s\S]+?)\n---` | 只保留正文 |
| **删除图片引用** `![alt](url)` | `gregex.ReplaceString` | 去掉外链图片 |
| **删除 Docusaurus 导航组件** `import DocCardList from '@theme/DocCardList';` 与 `<DocCardList />` | `gstr.Replace` × 2 | 去掉文档站外壳 |
| 折叠 3+ 连续换行为 2 个 | 正则 `[\n\r]{3,}` → `\n\n` | 排版整洁 |

输出:
- 单文件:清洗后的 Markdown,路径保持相对结构(如 `references/核心组件/数据库ORM/ORM链式操作.md`)
- 索引文件:`references/README.MD`(402 行,纯 Markdown 表格,链接 + description)

### 4.2 examples/(实战示例)— 接近整目录拷贝

扫描源:`/Users/john/Workspace/github/gogf/examples/`(对应 [github.com/gogf/examples](https://github.com/gogf/examples) 仓库)。

处理流程(完整还原):

| 处理动作 | 实现位置 | 说明 |
|---------|---------|------|
| 只挑文件名含 `ZH` 的 README | `gstr.Contains(gfile.Basename(file), "ZH")` | **只取中文版**,丢弃 EN |
| 校验目录必须是真实 Go 项目 | `isDirectExampleDirPath()` 检查 `.go` 或 `go.mod` | 过滤掉纯文档目录 |
| 解析 YAML front matter 取 description | 同上 | 用于索引表 |
| 跳过 `notSkillReference: true` | 同上 | — |
| **`gfile.CopyDir` 整目录复制** | `SaveExamples()` | **go.mod、main.go、所有源码、配置全部原样保留** |

输出:
- 单示例:完整 Go 项目(`main.go`、`go.mod`、`internal/`、`api/`、`Makefile` 等)
- 索引文件:`examples/README.MD`(53 行,纯 Markdown 表格)

### 4.3 与官方仓库的对照结论

| 维度 | references/ | examples/ |
|------|------------|-----------|
| 来源 | `gf-site/docs/` 的 .md | `examples/` 目录 |
| 处理对象 | 仅 .md | **整个目录**(代码 + 配置 + go.mod) |
| 清洗程度 | **中**:去 front matter、去图片、去 DocCardList | **低**:只挑 ZH 版 README,目录整拷 |
| 过滤规则 | 长度、行数、notSkillReference | 必须是 Go 项目、notSkillReference |
| 与官方差异 | 仅格式清洗,**正文一字不改** | **逐字节相同**(目录拷贝) |
| 加工产物 | 干净的 Markdown | 真实可运行的项目 |

**核心结论**:references 是 **"清洗过的 Markdown"**(约 380 份),examples 是 **"原样拷过来的 Go 工程"**(约 50 个)。加起来约 310 行 Go 代码的生成器——目标是 **让 Skill 跟随官方仓库同步**,维护者跑 `make skills` 就能刷新内容,而不是每次手动整理。

### 4.4 一个小瑕疵

`main.go` 中 `updateSkillFileContent()` 调用了:
```go
gstr.ReplaceByMap(skillTplContent, map[string]string{
    "{{SKILL_NAME}}": data.SkillName,
})
```
但 `SKILL.tpl.md` 中**根本不存在 `{{SKILL_NAME}}` 占位符**。所以这段字符串替换是**无效的**,等于把模板原封不动复制到 `skills/goframe-v2/SKILL.md`。这与 md5sum 验证一致(两个文件字节级相同),属于一段可清理的死代码。

---

## 五、AI 能否主动查阅 references/ 和 examples/?

这是用户问得最关键的问题。**答案分两层**:

### 5.1 现代 AI 的"自主探索能力"是有的,但有限

Claude、GPT-4 这类模型在看到 SKILL.md 末尾的两个链接时,**会**有相当大概率去打开 `references/README.MD` 和 `examples/README.MD`。但:
- **没有明确的"何时读"指令**,AI 只会**在需要更详细信息时**才会去探索
- 当 AI 觉得"我已经会用 ORM 写个 CRUD 了"时,**不会主动去看 380 份文档**
- 面对 402 行的扁平索引表,**很可能直接用训练数据编**而不是去 grep
- 完全没有"读哪份 reference → 解决哪类问题"的映射,AI 探索成本极高

### 5.2 SKILL.md 的引导能力评估

现有 SKILL.md 末尾的 5 行:
```markdown
# GoFrame Documentation
Complete GoFrame development resources covering component design, usage, best practices, 
and considerations: [GoFrame Documentation](./references/README.MD)

# GoFrame Code Examples
Rich practical code examples covering HTTP services, gRPC services, and various project 
types: [GoFrame Examples](./examples/README.MD)
```

**问题诊断**:
1. ❌ **没有触发条件**:AI 不知道"用户问什么时该去翻 references"
2. ❌ **没有分类映射**:380 份文档分属 11 个大类,但 SKILL.md 没给"读 ORM 文档去 `references/核心组件/数据库ORM/`"这种路由
3. ❌ **没有示例 prompt → 示例项目的对应**:用户说"加 JWT 鉴权",AI 不知道 `examples/httpserver/jwt/` 就是参考
4. ❌ **没有"哪些 reference 是必读"**:例如"新项目必读"应该指向 `references/开发工具/项目创建-init.md` 与 `references/框架设计/工程开发设计/工程目录设计.md`,但 SKILL.md 没说
5. ❌ **`references/README.MD` 没有目录分类**:402 行扁平表格,AI 没法一眼看出"哪类问题查哪一节"

**对比**:**`anthropics/skills/internal-comms/SKILL.md`** 仅 32 行就完成了同等任务:

```markdown
## How to use this skill
1. **Identify the communication type** from the request
2. **Load the appropriate guideline file** from the `examples/` directory:
   - examples/3p-updates.md - For Progress/Plans/Problems team updates
   - examples/company-newsletter.md - For company-wide newsletters
   - examples/faq-answers.md - For answering frequently asked questions
   - examples/general-comms.md - For anything else that doesn't explicitly match one of the above
3. **Follow the specific instructions** in that file for formatting, tone, and content gathering
```

这是 **"识别意图 → 路由到具体资源"** 的极简范式,GoFrame 完全有能力照搬——把 11 个 reference 分类与 ~10 个 example 类别做成类似的路由表。

### 5.3 实际行为分析(根据大模型行为研究)

| 场景 | AI 实际行为 | 原因 |
|------|------------|------|
| 用户问"goframe 怎么配置日志" | **大概率直接答**,不去查 references/日志组件 | SKILL.md 没说要查 |
| 用户说"用 gf gen 生成 DAO" | **可能**会去看 `references/开发工具/代码生成-gen/` | SKILL.md 第 11 行明确提到了此路径 |
| 用户说"我想做服务发现" | **完全不知道**该看 `references/微服务开发/服务注册发现.md` 还是 `examples/registry/etcd/` | SKILL.md 没说 |
| 用户问"软删除不生效" | **可能**会去读 Soft Delete 节 | SKILL.md 已强调此规则 |
| 用户问"如何组织大型项目目录" | **不知道**该看 `references/框架设计/工程开发设计/工程目录设计.md` | SKILL.md 没说 |

**结论**:**现有 SKILL.md 在 ORM/时间/软删除规范上对 AI 的引导是充分的**;但在 **references/ 与 examples/ 的探索指引上是几乎为零的**。这导致 380+ 份文档与 50+ 个示例项目实际利用率极低,大量宝贵的实战案例被埋在索引表里吃灰。

---

## 六、与行业最佳实践的对比

### 6.1 五个关键模式(来自 anthropics/skills、vercel-labs/agent-skills)

| 模式 | 代表案例 | GoFrame 现状 |
|------|---------|-------------|
| **意图 → 资源路由表** | `internal-comms/SKILL.md`(32 行)用"通信类型 → examples/xxx.md"映射 | ❌ 缺失 |
| **分阶段工作流** | `mcp-builder/SKILL.md` 用 4 阶段(Phase 1/2/3/4)+ 每个阶段引用不同 reference | ❌ 缺失 |
| **决策树** | `webapp-testing/SKILL.md` "是静态 HTML 吗?→ 是 → 直接读;否 → 服务在跑吗?" | ❌ 缺失 |
| **失败兜底话术** | `vercel-optimize/SKILL.md` 有完整 "If unsupported, stop and ask" 段落 | ❌ 缺失 |
| **硬性安全/规范** | `vercel-optimize/SKILL.md` "Never put auth tokens in shell commands" | ❌ 缺失 |

### 6.2 关键差距总结

GoFrame 在以下维度**高于**平均:
- ✅ frontmatter `description` 的精确度
- ✅ 反例/正例的代码对照
- ✅ YAML frontmatter 完整性
- ✅ 总行数控制(136 行 vs 规范 500 行上限)

GoFrame 在以下维度**低于**行业最佳实践:
- ❌ 路由表(最关键的缺失)
- ❌ 分阶段/决策树
- ❌ references/ 与 examples/ 的主动探索指引
- ❌ 失败兜底话术
- ❌ 工具使用指引(grep / read / list 的使用建议)

---

## 七、改进建议

按优先级排序,推荐如下增量改动(全部对 SKILL.tpl.md 现有结构无侵入性):

### 7.1 高优先级:补一段 20~30 行的"路由表"(单点修复,效果最大)

在 `Critical Conventions` 之前,新增一段:

```markdown
## How to navigate references and examples

When the user asks about a GoFrame topic, use this map **before writing code**:

| User intent | Read first |
|---|---|
| New project scaffolding | `references/开发工具/项目创建-init.md` |
| HTTP routes / middleware / request-response | `references/WEB服务开发/` |
| gRPC / service discovery / load balancing | `references/微服务开发/` |
| ORM / DAO / DO / Entity / soft delete / transactions | `references/核心组件/数据库ORM/` |
| Logging / config / error handling / cache / validation | `references/核心组件/{日志组件,配置管理,错误处理,缓存管理,数据校验}/` |
| CLI tools (`gf init`, `gf gen`, `gf build`, `gf run`) | `references/开发工具/` |
| Observability / tracing / metrics | `references/服务可观测性/` |
| Deployment (Docker / Nginx / supervisor) | `references/项目部署/` |

For end-to-end runnable projects, consult:
- HTTP canonical: `examples/practices/user-http-service/`
- gRPC canonical: `examples/practices/user-grpc-service/`
- Single-feature demos: `examples/{httpserver,grpc,database,nosql,...}/<topic>/`

**Workflow phases**:
1. **Project bootstrap**: `gf init` → `references/开发工具/项目创建-init.md`
2. **API definition & codegen**: `references/开发工具/代码生成-gen/` → `gf gen dao|ctrl|pb`
3. **Business logic**: `examples/practices/user-http-service` 作为模板
4. **Observability & deploy**: `references/服务可观测性/` + `references/项目部署/`
```

### 7.2 中优先级:补一段 "When to apply / When NOT to apply" 段落

```markdown
## When to apply

Apply these conventions when the project imports `github.com/gogf/gf/v2` and the
user is doing any of:
- Scaffolding a new HTTP/gRPC service with `gf init`
- Implementing controllers, services, DAOs in an existing GoFrame codebase
- Reviewing generated vs. hand-written code
- Database queries using `gdb` (DO objects, NOT `g.Map`)

Do **NOT** apply when:
- The project's `go.mod` does not require `github.com/gogf/gf/v2`
- The user is doing frontend, shell scripts, or pure Go libraries
- The user explicitly asks for Gin / Echo / Fiber / other frameworks

## Example prompts this skill handles

- "Build a user service with GoFrame, RESTful API, CRUD operations"
- "Add JWT authentication to my GoFrame project" → see `examples/httpserver/jwt/`
- "Why isn't my `CreatedAt` saving?" → see Soft Delete & Time Maintenance below
- "Generate DAO code for a new MySQL table" → see `references/开发工具/代码生成-gen/数据规范-gen dao.md`
- "Add gRPC service discovery with etcd" → see `examples/grpc/resolver/`
```

### 7.3 中优先级:补一段 "Stop and ask" 兜底话术

```markdown
## When to stop and ask

If you encounter any of these, **stop and ask the user** instead of inventing
an answer:

1. **Project doesn't import GoFrame**: "This project doesn't import
   `github.com/gogf/gf/v2`. Confirm whether to add GoFrame or use a different
   framework?"
2. **Frontend-only task**: "GoFrame is a backend framework; for frontend
   work, this skill doesn't apply."
3. **Conflicting framework signals**: "I see Gin and GoFrame imports in the
   same module. Please confirm the canonical framework before I proceed."
4. **Logic directory usage request**: "GoFrame convention is to put business
   logic in `internal/logic/` via codegen, not hand-write it. Confirm whether
   you want a custom layout."
```

### 7.4 低优先级:补充 `compatibility` 与 `metadata` 字段

```yaml
metadata:
  author: gogf
  version: "1.0.0"
compatibility: Requires Go 1.18+, optionally gf CLI v2.4+ for scaffolding.
```

### 7.5 长期改进:重写 README.MD 索引为"按类别分组 + 关键字"

把 `references/README.MD` 从 402 行扁平表格改为 **按 11 大类分组** + **每类下按关键字检索** 的二级结构。这样即使没有 SKILL.md 的路由指引,AI 也能快速定位。

---

## 八、总结

### 8.1 这个项目做得好的地方

1. **清晰的项目定位**:把 AI 编程规范文档化、工程化,填补了 GoFrame 在 AI Coding 时代的空白
2. **生成器架构合理**:让 Skill 跟随官方仓库同步,避免长期脱节;310 行 Go 代码实现 ETL,简洁有效
3. **数据规模可观**:380+ 份参考文档 + 50+ 实战示例,覆盖面完整
4. **"代码规范教学"扎实**:Critical Conventions 中的反例/正例对照堪称教材级
5. **frontmatter `description` 精确**:行业标杆级,触发条件与不触发条件都讲清
6. **Apache-2.0 开源**:对社区贡献友好

### 8.2 主要问题

1. **SKILL.md 的"导航"严重缺位**:有 400 份 reference 和 50 个 example,但 SKILL.md 没教 AI 何时去查哪一份
2. **缺乏分阶段工作流**:gf init → gf gen → 业务逻辑 → 部署 这条主线未结构化
3. **缺乏失败兜底**:非 GoFrame 项目、纯前端、跨框架冲突等情况无应对话术
4. **references/README.MD 是 402 行扁平表**:可读性与可检索性都有改进空间
5. **死代码**:`main.go` 的 `{{SKILL_NAME}}` 占位符替换无对应模板,纯属无效代码

### 8.3 适用人群结论

- **GoFrame 框架使用者**:✅ 强烈推荐安装,可显著提升 AI 生成 GoFrame 代码的规范性
- **ORM/CRUD 相关开发**:✅ 现有 SKILL.md 的 Critical Conventions 已经足够
- **HTTP/微服务/中间件相关开发**:⚠️ 需要 AI 自己探索 references/,效率打折扣
- **大型项目架构师**:⚠️ 缺乏分阶段工作流和工程目录设计的主动引导
- **跨框架混合场景**:❌ 缺乏"非 GoFrame 项目"的兜底话术

### 8.4 一句话评价

> **GoFrame Skills 是一个"内容丰富、导航缺失"的 Skill 包**——它把 GoFrame 官方资料工程化得很到位,但 SKILL.md 本身没有完成"教会 AI 如何使用这些资料"的关键使命。如果补上 7.1 ~ 7.3 建议的 ~60 行内容,可以从"良"升级到"优"。

---

## 附录 A:关键文件清单

| 文件 | 行数 | 作用 |
|------|----:|------|
| `SKILL.tpl.md` | 136 | Skill 模板 |
| `main.go` | 105 | 生成器入口 |
| `references/refences.go` | 150 | 参考资料生成 |
| `examples/examples.go` | 160 | 示例项目生成 |
| `skills/goframe-v2/SKILL.md` | 136 | 生成产物(=模板) |
| `skills/goframe-v2/references/README.MD` | 402 | 参考资料索引表 |
| `skills/goframe-v2/examples/README.MD` | 53 | 示例项目索引表 |
| `references/` 总文件数 | ~380 | 清洗后的 Markdown |
| `examples/` 项目数 | ~50 | 真实 Go 项目 |

## 附录 B:参考资料

- **Anthropic 官方 Skills**:https://github.com/anthropics/skills
- **Vercel Labs Agent Skills**:https://github.com/vercel-labs/agent-skills
- **Agent Skills 规范**:https://agentskills.io/specification
- **Skills.sh 目录**:https://skills.sh/
- **GoFrame 官方**:https://github.com/gogf/gf
- **GoFrame 文档源仓库**:https://github.com/gogf/gf-site
- **GoFrame 示例源仓库**:https://github.com/gogf/examples
